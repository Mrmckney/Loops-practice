const person = {
  name: "Matt",
  age: 20,
  inClass: true,
};

for (const eachProperty in person) {
  console.log(key); // displaying the property name
}

for (const eachProperty in object) {
  console.log(object[key]); // displaying the value of the property
}
